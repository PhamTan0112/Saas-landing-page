import React from "react";
import { Container } from "../shared/Container";

const Brands = () => {
  return (
    <section>
      <Container></Container>
    </section>
  );
};

export default Brands;
